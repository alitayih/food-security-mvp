# Repository Guidance

- Main app entrypoint: `streamlit_app.py`.
- Keep this as a single-service Streamlit MVP.
- Prefer reliability over feature complexity.
- Ensure demo mode keeps app usable without external APIs.
- Do not commit secrets; use Streamlit secrets/env vars.
